# 🎨 استراتيجية نقل التصميم (UI Migration Strategy)

**الهدف:** نقل الأصول المرئية القيمة من المستودع القديم (S-ACM-Project) إلى المستودع الجديد (UniCore-OS) دون نقل أي منطق برمجي سيء.

---

## 📜 المبادئ الأساسية

1.  **الفصل التام بين الشكل والوظيفة:** نحن ننسخ "الشكل" (Look) وليس "الوظيفة" (Function).
2.  **لا للنسخ الأعمى:** كل ملف يتم فحصه قبل نقله.
3.  **الأولوية للمكونات الذرية:** نبدأ بأصغر المكونات (Button, Input) ثم الأكبر.

---

## 🗺️ خطة النقل على 3 مراحل

### المرحلة الأولى: نقل مكونات `shadcn/ui` الأساسية

هذه هي أسهل وأهم مرحلة. بما أن المشروعين يستخدمان نفس المكتبة، سنقوم بإعادة تهيئتها في المشروع الجديد.

1.  **تهيئة `shadcn/ui`:** سنقوم بتشغيل الأمر `npx shadcn-ui@latest init` في مشروع `UniCore-OS`.
2.  **تثبيت المكونات المطلوبة:** بدلاً من نسخ الملفات، سنقوم بتثبيت المكونات التي نحتاجها مباشرة من `shadcn/ui` باستخدام الأمر `npx shadcn-ui@latest add [component]`.
    -   **قائمة المكونات:** `button`, `card`, `input`, `label`, `table`, `dropdown-menu`, `badge`, `alert`, `dialog`, `tabs`, `sonner`, `checkbox`, `select`, `accordion`, `textarea`.
3.  **النتيجة:** نحصل على مكونات نظيفة ومحدثة ومتوافقة 100% مع هيكلية Next.js.

### المرحلة الثانية: نقل المكونات المخصصة والتخطيط

هنا يكمن العمل اليدوي الدقيق. سنقوم بنقل المكونات التي تم بناؤها فوق `shadcn/ui` والتخطيط العام.

1.  **تحديد الأصول القيمة:**
    -   `DashboardLayout.tsx`: الهيكل الرئيسي للتطبيق (القائمة الجانبية + الشريط العلوي).
    -   `Sidebar.tsx`: القائمة الجانبية الاحترافية.
    -   `MobileNav.tsx`: الشريط السفلي للموبايل.
    -   `ThemeToggle.tsx`: زر تبديل الثيم.
    -   `UserNav.tsx`: قائمة المستخدم المنسدلة.

2.  **عملية النقل والتنظيف:**
    -   **النسخ:** سيتم نسخ كود JSX (جزء `return (...)`) من الملف القديم.
    -   **اللصق في الجديد:** سيتم لصق الكود في ملف جديد داخل `src/components/layout/` في `UniCore-OS`.
    -   **إعادة كتابة المنطق:** **لن يتم نسخ أي `useState`, `useEffect`, أو أي دالة أخرى.** سيتم إعادة كتابة كل المنطق من الصفر ليتوافق مع Next.js Server Components و Server Actions.
    -   **مثال (Sidebar):** سننسخ هيكل القائمة، لكن منطق تحديد الرابط "النشط" سيتم إعادة كتابته باستخدام `usePathname` من `next/navigation`.

### المرحلة الثالثة: إعادة بناء الصفحات

لن يتم نقل أي ملف من مجلد `pages/` القديم. سيتم إعادة بناء كل صفحة من الصفر باستخدام المكونات التي نقلناها في المرحلتين السابقتين.

1.  **إنشاء المسارات:** سيتم إنشاء مسارات جديدة في `src/app/` تتوافق مع المخطط الأزرق (e.g., `src/app/(dashboard)/users/page.tsx`).
2.  **إعادة بناء الواجهة:** سيتم استخدام المكونات الجاهزة (`Card`, `Table`, `Button`) لإعادة بناء واجهة كل صفحة بسرعة.
3.  **ربط البيانات:** سيتم ربط الواجهات مباشرة بـ Server Actions لجلب البيانات من قاعدة البيانات الحقيقية، بدلاً من `mockData.ts`.

---

## ⏳ الجدول الزمني التقديري

| المرحلة | المهمة | الوقت التقديري |
| :--- | :--- | :--- |
| 1 | تهيئة ونقل `shadcn/ui` | **ساعة واحدة** |
| 2 | نقل وتنظيف التخطيط العام | **3 ساعات** |
| 3 | إعادة بناء الصفحات الرئيسية (Users, Roles, Courses) | **6 ساعات** |
| **المجموع** | | **~10 ساعات عمل** |

---

بهذه الاستراتيجية، نضمن الحصول على أفضل ما في المشروع القديم (التصميم الجميل) مع التخلص الكامل من كل ديونه التقنية والمعمارية.
