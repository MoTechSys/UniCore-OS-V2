# 🚀 وثيقة التسليم والانتقال - UniCore-OS

**إلى الذكاء الاصطناعي القادم،**

أنا MAX، الذكاء الاصطناعي الذي بنى أساس هذا المشروع. مهمتي انتهت هنا، ومهمتك تبدأ الآن. هذه الوثيقة هي "مفتاح التشغيل" الخاص بك. اقرأها بدقة، فهي تحتوي على كل ما تحتاجه لإكمال المشروع بنفس الجودة والمعايير.

---

## 1. ملخص الحالة التقنية (Current State)

لقد تم بناء أساس متين وقابل للتطوير. الوضع الحالي للمشروع كالتالي:

| العنصر | التفاصيل |
|---|---|
| **المستودع** | `https://github.com/MoTechSys/UniCore-OS` |
| **التقنيات** | `Next.js 15+` (App Router), `NextAuth v5` (Beta), `Prisma 5`, `SQLite` (للتطوير) |
| **الإنجاز** | **35%** |
| **الحزم المكتملة** | ✅ المصادقة (Auth), ✅ إدارة المستخدمين (User Management), ✅ إدارة الأدوار (Role Management) |
| **الحالة** | الكود نظيف، موثق، ومرفوع بالكامل إلى المستودع. |

---

## 2. دستور العمل (The MAX Standards)

هذا المشروع يتبع معايير **MAX** الصارمة. **لا تهاون في هذه القواعد على الإطلاق.** أي خرق لها يعتبر فشلاً في المهمة.

#### 1. **No `any` Types (TypeScript)**
   - **ممنوع منعاً باتاً استخدام `any`**. كل متغير، دالة، أو كائن يجب أن يكون له نوع محدد (Typed). استخدم `zod` لتعريف الأنواع والتحقق منها.

#### 2. **Strict Server Actions**
   - **جميع عمليات (Create, Update, Delete) يجب أن تتم عبر Server Actions حصراً.**
   - كل Server Action يجب أن يبدأ بالتحقق من صلاحيات المستخدم (`requirePermission`).
   - كل Server Action يجب أن يستخدم `zod` للتحقق من المدخلات (`schema.parse(data)`).

#### 3. **Clean Architecture**
   - **افصل المنطق عن الواجهة.**
     - **`features/[feature]/actions`**: للـ Server Actions.
     - **`features/[feature]/components`**: للـ Client Components.
     - **`app/(dashboard)/[page]`**: للـ Server Components (صفحات العرض الرئيسية).

#### 4. **Permission Control**
   - **استخدم `requirePermission` داخل Server Actions** لحماية العمليات.
   - **استخدم `<PermissionGate permission="...">` في الواجهات** لإخفاء/إظهار المكونات بناءً على صلاحيات المستخدم.
   - **استخدم `hasPermission`** للتحقق من الصلاحيات داخل المكونات.

---

## 3. خارطة الطريق المتبقية (The Blueprint)

مهمتك هي إكمال الحزم من 4 إلى 10. اتبع هذا الدليل:

### ⏳ الحزمة 4: الهيكل الأكاديمي (Academic Structure)
   - **الفكرة:** دمج إدارة الكليات، الأقسام، والتخصصات في صفحة واحدة لتوفير الكود.
   - **التنفيذ المقترح:**
     1. أنشئ صفحة `/academic`.
     2. استخدم عرض شجري (Tree View) أو `Accordion` متداخل.
     3. **المستوى الأول:** قائمة الكليات.
     4. **المستوى الثاني (عند فتح كلية):** قائمة الأقسام التابعة لها.
     5. **المستوى الثالث (عند فتح قسم):** قائمة التخصصات التابعة له.
     6. كل عنصر في الشجرة يجب أن يكون قابلاً للتعديل والحذف (CRUD) عبر Modals.

### ⏳ الحزمة 5: إدارة الفصول (Class Management)
   - **الفكرة:** إدارة الفصول الدراسية وربط الطلاب بها.
   - **التنفيذ المقترح:**
     1. **إدارة الفصول الدراسية (Semesters):** صفحة بسيطة لإنشاء الفصول (e.g., "الفصل الدراسي الأول 2025-2026").
     2. **إدارة الشُعب (Sections):** عند اختيار فصل دراسي، تظهر الشعب المتاحة. كل شعبة تابعة لمقرر معين.
     3. **تسجيل الطلاب:** في صفحة كل شعبة، أضف واجهة لإضافة الطلاب (بحث بالاسم أو الرقم الجامعي) وربطهم بجدول `StudentSection`.

### ⏳ الحزمة 6: محرك الكويزات (Quiz Engine) - **الأكثر تعقيداً**
   - **الفكرة:** بناء نظام كويزات متكامل.
   - **هيكلية البيانات المقترحة:**
     - `Quiz`: يحتوي على اسم الكويز، إعداداته (الوقت، عدد المحاولات)، والمقرر التابع له.
     - `Question`: يتبع لـ `Quiz` ويحتوي على نص السؤال ونوعه (MCQ, T/F, Essay).
     - `Option`: يتبع لـ `Question` (في حالة MCQ) ويحتوي على نص الخيار وما إذا كان صحيحاً.
     - `QuizAttempt`: يسجل محاولة الطالب للكويز.
     - `StudentAnswer`: يسجل إجابة الطالب لكل سؤال في المحاولة.
   - **التنفيذ المقترح:**
     1. **إنشاء الكويز:** واجهة من عدة خطوات (Wizard) لإنشاء الكويز وإضافة الأسئلة.
     2. **بنك الأسئلة:** اسمح بإعادة استخدام الأسئلة بين الكويزات.
     3. **واجهة الطالب:** صفحة لتقديم الكويز مع عداد للوقت.

---

## 4. رسالة التشغيل (The Initialization Prompt)

**انسخ هذه الرسالة وألصقها في حسابك الجديد لبدء العمل فوراً:**

```
مرحباً، أنا الذكاء الاصطناعي الجديد المكلف بإكمال مشروع UniCore-OS.

لقد قرأت وثيقة التسليم والانتقال (MIGRATION_GUIDE.md) وأفهم تماماً حالة المشروع، المعايير الصارمة (MAX Standards)، وخارطة الطريق المتبقية.

**المستودع:** https://github.com/MoTechSys/UniCore-OS

**مهمتي الحالية:** البدء في تنفيذ "الحزمة 4: الهيكل الأكاديمي" كما هو موضح في الوثيقة.

**الخطة:**
1.  سأقوم بإنشاء صفحة `/academic`.
2.  سأبني واجهة عرض شجري (Tree View) لإدارة الكليات، الأقسام، والتخصصات.
3.  سأستخدم Server Actions و Modals لتنفيذ عمليات CRUD.
4.  سألتزم 100% بمعايير MAX (No any, Server Actions, Clean Architecture, Permission Control).

سأبدأ الآن. لا داعي لتكرار التعليمات، كل شيء واضح.
```

---

**مهمتك واضحة. أكمل المسيرة بنفس الجودة. بالتوفيق.**

**- MAX**
